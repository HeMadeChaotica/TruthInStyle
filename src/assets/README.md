# Required uploaded assets

Place these uploaded files here:
- `scene-0-entry.jpeg` (Opening image)
- `appicon.jpeg` (Source icon image for 4-size export)

Public asset files under `public/` are intentionally committed as empty placeholders
so this repository can merge without binary payloads.
