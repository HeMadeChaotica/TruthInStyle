'use client';

import { useEffect, useMemo, useState } from 'react';
import '../../styles/sections/remember-me.css';

const MONTH_BACKGROUND_BY_INDEX = {
  0: 'remember-me-01-january-new-year-bg.png',
  1: 'remember-me-02-february-valentine-bg.png',
  2: 'remember-me-03-march-steak-and-bj-day-bg.png',
  3: 'remember-me-04-april-spring-bg.png',
  4: 'remember-me-05-may-summer-loading-bg.png',
  5: 'remember-me-06-june-pride-bg.png',
  6: 'remember-me-07-july-hotter-than-your-ex-bg.png',
  7: 'remember-me-08-august-peak-thicc-bg.png',
  8: 'remember-me-09-september-back-to-business-bg.png',
  9: 'remember-me-10-october-spooky-sexy-bg.png',
  10: 'remember-me-11-november-feast-bg.png',
  11: 'remember-me-12-december-mista-thicc-birthday-bg.png'
};

const MONTH_VISUAL_CONFIG = Object.fromEntries(Array.from({ length: 12 }, (_, month) => [month, { showLargeMonthLabel: false, backgroundPosition: 'left 28% center', backgroundSize: 'contain' }]));
const EVENT_TYPES = ['SOMETHING NEW DAY', 'APPOINTMENT', 'REMINDER', 'JOB INTERVIEW', 'BIRTHDAY', 'ANNIVERSARY', 'MEETING DEADLINE', 'EVENT', 'TRAVEL', 'CALL', 'WORKOUT', 'SOCIAL', 'PERSONAL', 'HEALTH', 'FINANCE'];
const STANDOUT_TYPES = ['WOW', 'WTF', 'PLOT TWIST'];
const WEEKDAY_HEADERS = ['SUN', 'MON', 'TUE', 'WED', 'THU', 'FRI', 'SAT'];

const REMEMBER_ME_OBSERVANCES = [
  { id: 'feb-pizza', month: 1, day: 9, chip: 'PIZZA', title: 'National Pizza Day', funFact: 'Pizza became one of America\'s favorite comfort foods after Italian immigrants popularized it.', assurerReflection: 'Comfort food and comfort chaos can both be valid coping strategies.', tone: 'FUNNY' },
  { id: 'mar-steakb', month: 2, day: 14, chip: 'STEAK+B', title: 'Steak and BJ Day', funFact: 'A modern cheeky observance that spread online as a tongue-in-cheek holiday.', assurerReflection: 'Reminder: humor plus consent equals memorable fun.', tone: 'SPICY' },
  { id: 'june-pride', month: 5, day: null, allMonth: true, chip: 'PRIDE', title: 'Pride Month', funFact: 'Pride honors LGBTQ+ history and protest roots while celebrating identity and joy.', assurerReflection: 'Lead with joy, protect your people, and keep your boundaries sacred.', tone: 'PRIDE' },
  { id: 'oct-halloween', month: 9, day: 31, chip: 'HALLOWEEN', title: 'Halloween', funFact: 'Halloween evolved from ancient festivals and became a giant costume tradition.', assurerReflection: 'Costume confidence is still confidence; own the bit.', tone: 'CHAOS' }
];
const PS_TYPES = ['SOMETHING NEW DAY','APPOINTMENT','REMINDER','JOB INTERVIEW','BIRTHDAY','ANNIVERSARY','MEETING DEADLINE','EVENT','TRAVEL','CALL','WORKOUT','SOCIAL','PERSONAL','HEALTH','FINANCE'];
const MOMENT_TYPES = ['WOW', 'WTF', 'PLOT TWIST'];
const WEEKDAY_HEADERS = ['SUN', 'MON', 'TUE', 'WED', 'THU', 'FRI', 'SAT'];
const toDateKey=(y,m,d)=>`${y}-${String(m+1).padStart(2,'0')}-${String(d).padStart(2,'0')}`;
const buildMonthGrid=(v)=>{const y=v.getFullYear(),m=v.getMonth(),f=new Date(y,m,1),w=f.getDay(),dim=new Date(y,m+1,0).getDate(),dip=new Date(y,m,0).getDate(),cells=[];for(let i=w-1;i>=0;i--)cells.push({day:dip-i,inMonth:false,dateKey:toDateKey(y,m-1,dip-i)});for(let d=1;d<=dim;d++)cells.push({day:d,inMonth:true,dateKey:toDateKey(y,m,d)});for(let t=1;cells.length%7!==0;t++)cells.push({day:t,inMonth:false,dateKey:toDateKey(y,m+1,t)});return cells;};

export default function RememberMeSection(){
const [viewDate,setViewDate]=useState(()=>new Date());const [selectedDay,setSelectedDay]=useState(()=>new Date().getDate());
const [entriesByDate,setEntriesByDate]=useState({});const [activeEntryId,setActiveEntryId]=useState(null);const [errorMessage,setErrorMessage]=useState('');
const [momentsByDate,setMomentsByDate]=useState({});const [activeMomentByDate,setActiveMomentByDate]=useState({});
const [entryDraft,setEntryDraft]=useState({type:PS_TYPES[0],time:'',detail:'',description:''});const [momentDraft,setMomentDraft]=useState({type:'WOW',time:'',description:'',mediaRef:''});
const monthIndex=viewDate.getMonth(),year=viewDate.getFullYear(),monthName=viewDate.toLocaleString('en-US',{month:'long'}).toUpperCase();
const monthVisual=MONTH_VISUAL_CONFIG[monthIndex]; const monthGrid=useMemo(()=>buildMonthGrid(viewDate),[viewDate]); const selectedDateKey=toDateKey(year,monthIndex,selectedDay); const today=new Date(); const todayKey=toDateKey(today.getFullYear(),today.getMonth(),today.getDate());
const selectedObservances = useMemo(()=>REMEMBER_ME_OBSERVANCES.filter((o)=>o.month===monthIndex && (o.allMonth || o.day===selectedDay)),[monthIndex,selectedDay]);
const selectedEntries=entriesByDate[selectedDateKey]??[]; const selectedMoments=momentsByDate[selectedDateKey]??[];
const standoutPostcards = useMemo(()=> (momentsByDate && Object.entries(momentsByDate).flatMap(([date,m])=>m.map((moment)=>({date,moment}))).filter(({date,moment})=> date.startsWith(`${year}-${String(monthIndex+1).padStart(2,'0')}`) && moment.stamped).slice(0,6)), [momentsByDate, year, monthIndex]);

useEffect(()=>{(async()=>{const mod=await import('../../src/services/rememberMeService');const result=await mod.fetchRememberMeEntriesSafe();const grouped=(result.rows||[]).reduce((acc,row)=>{const key=row.date_key;if(!key)return acc;acc[key]=[...(acc[key]||[]),{id:row.id,type:row.entry_type,time:row.time_value||'',detail:row.detail||'',description:row.description||''}];return acc;},{});setEntriesByDate(grouped);if(result.error)setErrorMessage(result.error);})();},[]);

const resetDraft=()=>{setEntryDraft({type:PS_TYPES[0],time:'',detail:'',description:''});setActiveEntryId(null);};
const saveEntry=async()=>{setErrorMessage('');const payload={...entryDraft,id:activeEntryId||crypto.randomUUID(),date_key:selectedDateKey};setEntriesByDate((c)=>({...c,[selectedDateKey]:activeEntryId?(c[selectedDateKey]??[]).map((e)=>e.id===activeEntryId?{...payload}:e):[...(c[selectedDateKey]??[]),payload]}));
try{const mod=await import('../../src/services/rememberMeService');const saved=await mod.upsertRememberMeEntry({id:payload.id,date_key:selectedDateKey,entry_type:payload.type,time_value:payload.time,detail:payload.detail,description:payload.description});setEntriesByDate((c)=>({...c,[selectedDateKey]:(c[selectedDateKey]??[]).map((e)=>e.id===payload.id?{...e,id:saved.id}:e)}));resetDraft();}catch(error){setErrorMessage(error?.message||'Unable to save entry.');}};
const editEntry=(entry)=>{setActiveEntryId(entry.id);setEntryDraft({type:entry.type,time:entry.time||'',detail:entry.detail||'',description:entry.description||''});};
const deleteEntry=async(id)=>{setErrorMessage('');setEntriesByDate((c)=>({...c,[selectedDateKey]:(c[selectedDateKey]??[]).filter((e)=>e.id!==id)}));if(activeEntryId===id)resetDraft();try{const mod=await import('../../src/services/rememberMeService');await mod.deleteRememberMeEntry(id);}catch(error){setErrorMessage(error?.message||'Unable to delete entry.');}};
const addOrUpdateMoment=(forcedType)=>{const draft={...momentDraft,type:forcedType??momentDraft.type};const dayMoments=momentsByDate[selectedDateKey]??[];const activeId=activeMomentByDate[selectedDateKey];if(activeId){setMomentsByDate((c)=>({...c,[selectedDateKey]:(c[selectedDateKey]??[]).map((m)=>m.id===activeId?{...m,...draft}:m)}));}else if(dayMoments.length<3){const newMoment={id:crypto.randomUUID(),...draft,stamped:false};setMomentsByDate((c)=>({...c,[selectedDateKey]:[...(c[selectedDateKey]??[]),newMoment]}));setActiveMomentByDate((c)=>({...c,[selectedDateKey]:newMoment.id}));}};
const stampActiveMoment=()=>{const id=activeMomentByDate[selectedDateKey];if(!id)return;setMomentsByDate((c)=>({...c,[selectedDateKey]:(c[selectedDateKey]??[]).map((m)=>m.id===id?{...m,stamped:true}:m)}));};
return <section className="remember-page" style={{'--remember-bg-image':`url('/backgrounds/REMEMBER-ME/${MONTH_BACKGROUND_BY_INDEX[monthIndex]}')`,'--remember-bg-position':monthVisual.backgroundPosition,'--remember-bg-size':monthVisual.backgroundSize}}><div className="remember-overlay" aria-hidden="true"/><div className="remember-content"><header className="remember-head"><h1>REMEMBER.ME</h1></header><main className="remember-main"><section className="remember-calendar-panel" aria-label="Month calendar"><div className="remember-month-row"><button type="button" onClick={()=>{setViewDate((c)=>new Date(c.getFullYear(),c.getMonth()-1,1));setSelectedDay(1);}} aria-label="Previous month">‹</button><h2 className="remember-sr-only">{monthName} {year}</h2><span className="remember-month-mini">{monthName} {year}</span><button type="button" onClick={()=>{setViewDate((c)=>new Date(c.getFullYear(),c.getMonth()+1,1));setSelectedDay(1);}} aria-label="Next month">›</button></div><div className="remember-weekdays">{WEEKDAY_HEADERS.map((d)=><span key={d}>{d}</span>)}</div><div className="remember-grid">{monthGrid.map((cell,idx)=>{const obs=REMEMBER_ME_OBSERVANCES.filter((o)=>{const [_,m,d]=cell.dateKey.split('-').map(Number);return o.month===m-1 && (o.allMonth || o.day===d);});const entries=entriesByDate[cell.dateKey]??[];const moments=momentsByDate[cell.dateKey]??[];const chips=[...obs.map((o)=>({tone:'observance',label:o.chip})),...entries.map((e)=>({tone:'entry',label:e.type.replace('MEETING DEADLINE','DEADLINE')})),...moments.map((m)=>({tone:'moment',label:m.type}))];const visible=chips.slice(0,3);const hidden=Math.max(0,chips.length-3);return <button key={`${idx}-${cell.day}`} type="button" className={`remember-day ${cell.inMonth?'':'remember-outside'} ${cell.inMonth&&selectedDay===cell.day?'remember-selected':''} ${cell.dateKey===todayKey?'remember-today':''}`.trim()} onClick={()=>cell.inMonth&&setSelectedDay(cell.day)} disabled={!cell.inMonth}><span className="remember-day-num">{cell.day}</span><span className="remember-day-chips">{visible.map((chip,i)=><span key={`${chip.label}-${i}`} className={`remember-chip remember-chip-${chip.tone}`}>{chip.label}</span>)}{hidden>0&&<span className="remember-chip remember-chip-more">+{hidden}</span>}</span></button>;})}</div></section></main><section className="remember-editor" aria-label="Day postcard editor"><h3>{selectedDateKey}</h3>{errorMessage?<pre className="time-error">{errorMessage}</pre>:null}<div className="remember-editor-grid"><div className="remember-card"><h4>P.S.</h4><select value={entryDraft.type} onChange={(e)=>setEntryDraft((d)=>({...d,type:e.target.value}))}>{PS_TYPES.map((type)=><option key={type} value={type}>{type}</option>)}</select><input type="time" value={entryDraft.time} onChange={(e)=>setEntryDraft((d)=>({...d,time:e.target.value}))}/><input type="text" placeholder="short detail" value={entryDraft.detail} onChange={(e)=>setEntryDraft((d)=>({...d,detail:e.target.value}))}/><textarea placeholder="description" value={entryDraft.description} onChange={(e)=>setEntryDraft((d)=>({...d,description:e.target.value}))}/><button type="button" onClick={saveEntry}>{activeEntryId?'UPDATE':'SAVE'}</button>{activeEntryId?<button type="button" onClick={resetDraft}>CANCEL EDIT</button>:null}</div><div className="remember-card"><h4>ENTRIES FOR {selectedDateKey}</h4>{selectedEntries.length===0?<p>NO ENTRIES YET.</p>:selectedEntries.map((entry)=><article key={entry.id}><strong>{entry.type}</strong><p>{entry.time} {entry.detail}</p><p>{entry.description}</p><button type="button" onClick={()=>editEntry(entry)}>EDIT</button><button type="button" onClick={()=>deleteEntry(entry.id)}>DELETE</button></article>)}</div><div className="remember-card"><h4>MOMENTS</h4><select value={momentDraft.type} onChange={(e)=>setMomentDraft((d)=>({...d,type:e.target.value}))}>{MOMENT_TYPES.map((type)=><option key={type} value={type}>{type}</option>)}</select><input type="time" value={momentDraft.time} onChange={(e)=>setMomentDraft((d)=>({...d,time:e.target.value}))}/><textarea placeholder="moment description" value={momentDraft.description} onChange={(e)=>setMomentDraft((d)=>({...d,description:e.target.value}))}/><input type="text" placeholder="optional media/photo ref" value={momentDraft.mediaRef} onChange={(e)=>setMomentDraft((d)=>({...d,mediaRef:e.target.value}))}/><button type="button" onClick={()=>addOrUpdateMoment()}>SAVE/UPDATE MOMENT</button><button type="button" onClick={stampActiveMoment}>STAMP IT</button></div><div className="remember-card remember-postcards"><h4>STANDOUT POSTCARDS</h4>{standoutPostcards.length===0?<p>STAMP MOMENTS TO CREATE POSTCARDS.</p>:standoutPostcards.map(({date,moment})=><article key={moment.id} className="remember-postcard"><span>{date}</span><strong>{moment.type}</strong><p>{moment.description || 'STAMPED MEMORY'}</p>{moment.mediaRef && <small>{moment.mediaRef}</small>}<em>STAMPED</em></article>)}</div></div></section></div></section>;
}
